name: Run the Nextstrain public builds

on:
  workflow_dispatch:
    inputs:
      dockerImage:
        description: "Specific container image to use for build (will override the default of `nextstrain build`)"
        required: false
        type: string

jobs:
  run-build:
    permissions:
      id-token: write
    uses: nextstrain/.github/.github/workflows/pathogen-repo-build.yaml@master
    secrets: inherit
    with:
      runtime: aws-batch
      env: |
        NEXTSTRAIN_DOCKER_IMAGE: ${{ inputs.dockerImage }}
      # Aiming to use c7a.8xlarge instances with 32 CPUs and 64GiB system memory
      # We need to define a lower amount of memory since the instance requires some overhead memory.
      # Previous commentary in <https://github.com/nextstrain/seasonal-flu/pull/298#discussion_r2729308445>
      # But c7a instances will be more performant than c5 instances
      run: |
        nextstrain build \
          --detach \
          --cpus 32 \
          --memory 60GiB \
          . \
          deploy_all \
          -p \
          --configfile profiles/nextstrain-public.yaml
